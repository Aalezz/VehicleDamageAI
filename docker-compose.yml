services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./models:/srv/models:ro
      - dbdata:/srv/data
    restart: unless-stopped
    # Uncomment for NVIDIA GPU inference:
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]

volumes:
  dbdata:
